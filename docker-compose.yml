version: '3.8'

services:
  # Actian VectorAI DB — TBD: replace with official image once confirmed
  # vectorai-db:
  #   image: actian/vectorai-db:latest
  #   ports:
  #     - "8100:8100"
  #   volumes:
  #     - vectorai-data:/data
  #   restart: unless-stopped

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - VECTORAI_HOST=vectorai-db
      - VECTORAI_PORT=8100
      - DEMO_MODE=true
    volumes:
      - ./backend:/app
      - ./uploads:/app/uploads
    # depends_on:
    #   - vectorai-db
    restart: unless-stopped

volumes:
  vectorai-data:
